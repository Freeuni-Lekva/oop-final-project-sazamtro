package bean.Questions;

public class MultiSelect extends ChoiceQuestions {

    public MultiSelect(QuestionType type, int id, String questionText, int position, int questionScore) {
        super(type, id, questionText, position, questionScore);
    }
}
